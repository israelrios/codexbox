[containers]
userns="host"
ipcns="host"
utsns="host"
cgroupns="host"
cgroups="disabled"
log_driver = "k8s-file"

[network]
default_rootless_network_cmd = "pasta"

[engine]
cgroup_manager = "cgroupfs"
events_logger="file"
runtime="crun"
